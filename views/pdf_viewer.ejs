<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/image/logo.png" type="image/icon type">
    <title>OnlineStudy</title>
    <link rel="stylesheet" href="/css/pdf_viewer.css">

    <!-- --bootstrap-- -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <!-- special javascript and css for disable advance options -->
    <script type="text/javascript" src="/javascript/disableDevoloper.js"></script>
    <link rel="stylesheet" href="/css/disableDevoloper.css">

    <!-- script to disable screenshot or print -->
    <script>
        document.addEventListener("keyup", function (e) {
            var keyCode = e.keyCode ? e.keyCode : e.which;
            if (keyCode == 44) {
                // stopPrntScr();
                hideDisplay1 = document.querySelector(".hideDisplay");
                hideDisplay1.style.display = "none";
                alert("You are trying to take screenshot");
            }
        });
    </script>
</head>
<body class="hideDisplay">

    <!-- external javascript to disable devoloper or advance features -->
    <div id="amm_drcfw_toast_msg"></div>

    <!-- bootstarp alert for warning -->
    <div class="alert alert-warning alert-dismissible fade show" role="alert" 
    style="position: fixed; top: 2vh; margin: 5px; width: 95%; right: 1vh; z-index: 99;">
        <strong style="color: crimson;"><u>WARNING!</u></strong><br>Dear, <%= student_user.first_name %> <%= student_user.last_name %>
        this pdf contains <strong style="color: crimson;">COPYRIGHT</strong>. So 
        <strong style="color: crimson;">don't try to print or take screenshot or share</strong>.
        If we find any copy of this pdf along with your ID and NAME, immidiate action will be taken.
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>

    <!-- ------------testing pdf link and hiding url----------- -->
    <div class="wrapper">
        <div class="hidelogo"></div>
        <span class="watermark">
            <%= student_user.student_id %>: <%= student_user.first_name %> <%= student_user.last_name %>
        </span>
        <iframe width='100%' height='100%' frameborder='0' allowfullscreen
            src="<%= pdf_url %>" src=''></iframe>
    </div>
    <!-- ------------ending here testing pdf link and hiding url----------- -->

    <!-- --bootstrap-- -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
</body>
</html>